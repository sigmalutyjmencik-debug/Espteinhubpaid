-- Project Astra v22.1 | t.me/epsteinhub
_G.A = {}
local A = _G.A

do
    local UIS = game:GetService("UserInputService")
    local Players = game:GetService("Players")
    local Lighting = game:GetService("Lighting")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local RS = game:GetService("RunService")
    local Debris = game:GetService("Debris")
    local SoundService = game:GetService("SoundService")
    local HttpService = game:GetService("HttpService")
    local LP = Players.LocalPlayer
    local Camera = workspace.CurrentCamera
    local GUI_PARENT
    pcall(function() GUI_PARENT = game:GetService("CoreGui") end)
    if not GUI_PARENT then GUI_PARENT = LP:WaitForChild("PlayerGui") end

    A.UIS = UIS
    A.Players = Players
    A.Lighting = Lighting
    A.RS = RS
    A.Debris = Debris
    A.SoundService = SoundService
    A.ReplicatedStorage = ReplicatedStorage
    A.HttpService = HttpService
    A.LP = LP
    A.Camera = Camera
    A.GUI_PARENT = GUI_PARENT

    for _, n in ipairs({"ProjectAstra","AstraFOV","AstraFreezeUI","AstraTPButton","AstraAirWalkButton","AstraColorPicker","GradientKeybinds","AstraSide"}) do
        local old = GUI_PARENT:FindFirstChild(n)
        if old then old:Destroy() end
    end

    A.BG = Color3.fromRGB(20,20,24)
    A.PANEL = Color3.fromRGB(24,24,29)
    A.DROPBG = Color3.fromRGB(22,22,27)
    A.BARBG = Color3.fromRGB(28,28,34)
    A.CHECKBG = Color3.fromRGB(26,26,31)
    A.WHITE = Color3.fromRGB(235,235,235)
    A.GRAY = Color3.fromRGB(145,145,150)
    A.ACCENT = Color3.fromRGB(255,25,120)

    A.SILENT_AIM = false
    A.HIT_CHANCE = 95
    A.HIT_PART = "Head"
    A.FOV_RADIUS = 200
    A.SHOW_FOV = true
    A.TEAM_CHECK = false
    A.SNAPLINE = false
    A.TRACER = false
    A.TRACER_TYPE = "Red Laser"
    A.TRACER_LIFE = 3
    A.RAPID_FIRE = false
    A.RAPID_VALUE = 10000000
    A.HIT_SOUND = false
    A.HIT_SOUND_ID = "121566025787365"
    A.HIT_SOUND_VOL = 0.3
    A.HIT_SOUND_PRESETS = {
        ["Rust"] = "121566025787365",
        ["Minecraft"] = "73369656122118",
        ["Rthro"] = "109681634329245",
        ["Minecraft bow"] = "135478009117226",
        ["Skeet"] = "140247876667835",
        ["Neverlose"] = "139452805868562",
        ["Untimity"] = "138086148728606",
    }
    A.silentAimFovColor = Color3.fromRGB(255,80,180)
    A.silentAimSnaplineColor = Color3.fromRGB(255,0,255)
    A.hitMarkerEnabled = true
    A.hitMarkerColor = Color3.fromRGB(255,0,0)
    A.hitMarkerSize = 9
    A.hitMarkerGap = 4
    A.hitMarkerDuration = 0.35
    A.ActiveHitmarkers = {}
    A.JUMP_SHOT = false
    A.instantHitEnabled = false
    A.noRecoilEnabled = false
    A.noSpreadEnabled = false
    A.instantReloadEnabled = false
    A.autoReloadEnabled = false
    A.noEquipDelayEnabled = false
    A.bulletSpeedValue = 5000
    A.fullAutoEnabled = false
    A.lastReloadTime = 0
    A.cameraFovEnabled = false
    A.cameraFovValue = 120
    A.noFogEnabled = false
    A.fullbrightEnabled = false
    A.gammaEnabled = false
    A.gammaValue = 10
    A.gammaColor = Color3.fromRGB(128,128,128)
    A.fogEnabled = false
    A.fogStart = 100
    A.fogEnd = 500
    A.fogColor = Color3.fromRGB(128,128,128)
    A.espEnabled = false
    A.espBox = true
    A.espName = true
    A.espHealth = true
    A.espDistance = true
    A.espWeapon = true
    A.espBoxColor = Color3.fromRGB(255,255,255)
    A.espNameColor = Color3.fromRGB(255,255,255)
    A.espDistColor = Color3.fromRGB(255,255,255)
    A.espWeapColor = Color3.fromRGB(255,200,0)
    A.espCache = {}
    A.customHandsEnabled = false
    A.customHandsColor = Color3.fromRGB(160,32,240)
    A.customHandsTransp = 0.55
    A.activeLockedArms = {}
    A.viewModelEnabled = false
    A.viewModelX = 5
    A.viewModelY = -2
    A.viewModelZ = -8
    A.viewModelConn = nil
    A.gunChamsEnabled = false
    A.gunChamsColor = Color3.fromRGB(255,80,255)
    A.skyboxEnabled = false
    A.skyboxMode = "None"
    A.activeSky = nil
    A.SKYBOXES = {
        ["Aurorka"] = "rbxassetid://159454299",
        ["Galaxy"] = "MULTI",
        ["Nebula"] = "rbxassetid://16932975940",
        ["Nebula 2"] = "rbxassetid://18618101697",
        ["Orion"] = "rbxassetid://4957505424",
    }
    A.speedHackEnabled = false
    A.speedValue = 50
    A.infJumpEnabled = false
    A.noClipEnabled = false
    A.bhopEnabled = false
    A.flyEnabled = false
    A.flySpeed = 50
    A.freezeEnabled = false
    A.freezeTargetCF = nil
    A.tpDistance = 100
    A.tpKey = Enum.KeyCode.T
    A.airWalkDuration = 3
    A.airWalkHeight = 100
    A.airWalkKey = Enum.KeyCode.V
    A.airWalkBusy = false
    A.whitelistEnabled = false
    A.whitelistPlayers = {}
    A.CONFIG_FOLDER = "ProjectAstraConfigs"
    A.keybinds = {
        SilentAim = Enum.KeyCode.Z,
        Fly = Enum.KeyCode.F,
        Freeze = Enum.KeyCode.X,
    }
    A.showKeybindOverlay = false
end

do
    local A = _G.A
    local WHITE = A.WHITE
    local ACCENT = A.ACCENT
    local PANEL = A.PANEL
    local DROPBG = A.DROPBG
    local BARBG = A.BARBG
    local CHECKBG = A.CHECKBG
    local UIS = A.UIS

    A.Stroke = function(parent, thickness)
        local s = Instance.new("UIStroke")
        s.Color = ACCENT
        s.Thickness = thickness or 1
        s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        s.Parent = parent
        return s
    end

    A.MakeText = function(parent, value, pos, size, ts, col)
        local l = Instance.new("TextLabel")
        l.Position = pos
        l.Size = size
        l.BackgroundTransparency = 1
        l.BorderSizePixel = 0
        l.Text = value
        l.TextColor3 = col or WHITE
        l.Font = Enum.Font.Code
        l.TextSize = ts or 13
        l.TextXAlignment = Enum.TextXAlignment.Left
        l.TextYAlignment = Enum.TextYAlignment.Center
        l.Parent = parent
        return l
    end

    A.Checkbox = function(parent, label, default, callback)
        local container = Instance.new("Frame")
        container.Size = UDim2.new(1,0,0,24)
        container.BackgroundTransparency = 1
        container.Parent = parent
        local box = Instance.new("TextButton")
        box.Size = UDim2.fromOffset(18,18)
        box.Position = UDim2.fromOffset(0,3)
        box.BackgroundColor3 = CHECKBG
        box.BorderSizePixel = 0
        box.Text = ""
        box.AutoButtonColor = false
        box.Parent = container
        A.Stroke(box,1)
        local fill = Instance.new("Frame")
        fill.Position = UDim2.fromOffset(3,3)
        fill.Size = UDim2.fromOffset(12,12)
        fill.BackgroundColor3 = ACCENT
        fill.BorderSizePixel = 0
        fill.Visible = default and true or false
        fill.Parent = box
        A.MakeText(container, label, UDim2.fromOffset(26,0), UDim2.new(1,-26,1,0), 13, WHITE)
        local state = default
        box.MouseButton1Click:Connect(function()
            state = not state
            fill.Visible = state
            if callback then pcall(callback, state) end
        end)
        return container
    end

    A.Slider = function(parent, label, minV, maxV, default, callback, suffix)
        suffix = suffix or ""
        local container = Instance.new("Frame")
        container.Size = UDim2.new(1,0,0,44)
        container.BackgroundTransparency = 1
        container.Parent = parent
        A.MakeText(container, label, UDim2.fromOffset(0,0), UDim2.new(1,-60,0,18), 12, WHITE)
        local valLabel = A.MakeText(container, tostring(default)..suffix, UDim2.new(1,-60,0,0), UDim2.fromOffset(60,18), 12, ACCENT)
        valLabel.TextXAlignment = Enum.TextXAlignment.Right
        local bar = Instance.new("Frame")
        bar.Position = UDim2.fromOffset(0,22)
        bar.Size = UDim2.new(1,0,0,18)
        bar.BackgroundColor3 = BARBG
        bar.BorderSizePixel = 0
        bar.Parent = container
        A.Stroke(bar,1)
        local ratio = maxV > minV and (default - minV)/(maxV - minV) or 0
        local fill = Instance.new("Frame")
        fill.Size = UDim2.new(ratio,0,1,0)
        fill.BackgroundColor3 = ACCENT
        fill.BorderSizePixel = 0
        fill.Parent = bar
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.fromScale(1,1)
        btn.BackgroundTransparency = 1
        btn.Text = ""
        btn.ZIndex = 5
        btn.Parent = bar
        local dragging = false
        local function update(input)
            local sizeX = bar.AbsoluteSize.X
            if sizeX <= 0 then return end
            local x = input.Position.X - bar.AbsolutePosition.X
            local r = math.clamp(x/sizeX, 0, 1)
            fill.Size = UDim2.new(r,0,1,0)
            local v = math.floor(minV + (maxV-minV)*r + 0.5)
            valLabel.Text = tostring(v)..suffix
            if callback then pcall(callback, v) end
        end
        btn.InputBegan:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                dragging = true
                update(i)
            end
        end)
        UIS.InputChanged:Connect(function(i)
            if dragging and (i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch) then
                update(i)
            end
        end)
        UIS.InputEnded:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        return container
    end

    A.Dropdown = function(parent, label, options, default, callback)
        local container = Instance.new("Frame")
        container.Size = UDim2.new(1,0,0,52)
        container.BackgroundTransparency = 1
        container.ClipsDescendants = false
        container.ZIndex = 50
        container.Parent = parent
        A.MakeText(container, label, UDim2.fromOffset(0,0), UDim2.new(1,0,0,18), 12, WHITE)
        local drop = Instance.new("Frame")
        drop.Position = UDim2.fromOffset(0,20)
        drop.Size = UDim2.new(1,0,0,28)
        drop.BackgroundColor3 = DROPBG
        drop.BorderSizePixel = 0
        drop.ZIndex = 51
        drop.Parent = container
        A.Stroke(drop,1)
        local selText = A.MakeText(drop, default or (options[1] or ""), UDim2.fromOffset(8,0), UDim2.new(1,-30,1,0), 12, WHITE)
        selText.ZIndex = 52
        local arrow = A.MakeText(drop, "v", UDim2.new(1,-22,0,0), UDim2.fromOffset(20,28), 11, WHITE)
        arrow.TextXAlignment = Enum.TextXAlignment.Center
        arrow.ZIndex = 52
        local click = Instance.new("TextButton")
        click.Size = UDim2.fromScale(1,1)
        click.BackgroundTransparency = 1
        click.Text = ""
        click.ZIndex = 55
        click.Parent = drop
        local menu = Instance.new("Frame")
        menu.Position = UDim2.fromOffset(0,29)
        menu.Size = UDim2.new(1,0,0,#options*24)
        menu.BackgroundColor3 = DROPBG
        menu.BorderSizePixel = 0
        menu.Visible = false
        menu.ZIndex = 100
        menu.Parent = drop
        A.Stroke(menu,1)
        for i,opt in ipairs(options) do
            local ob = Instance.new("TextButton")
            ob.Position = UDim2.fromOffset(0,(i-1)*24)
            ob.Size = UDim2.new(1,0,0,24)
            ob.BackgroundColor3 = DROPBG
            ob.BorderSizePixel = 0
            ob.Text = opt
            ob.TextColor3 = WHITE
            ob.Font = Enum.Font.Code
            ob.TextSize = 12
            ob.TextXAlignment = Enum.TextXAlignment.Left
            ob.AutoButtonColor = false
            ob.ZIndex = 101
            ob.Parent = menu
            Instance.new("UIPadding",ob).PaddingLeft = UDim.new(0,8)
            ob.MouseButton1Click:Connect(function()
                selText.Text = opt
                menu.Visible = false
                if callback then pcall(callback, opt) end
            end)
        end
        click.MouseButton1Click:Connect(function()
            menu.Visible = not menu.Visible
        end)
        return container
    end

    A.KeybindRow = function(parent, label, default, setter)
        local container = Instance.new("Frame")
        container.Size = UDim2.new(1,0,0,24)
        container.BackgroundTransparency = 1
        container.Parent = parent
        A.MakeText(container, label, UDim2.fromOffset(0,0), UDim2.new(1,-80,1,0), 13, WHITE)
        local kb = Instance.new("TextButton")
        kb.AnchorPoint = Vector2.new(1,0)
        kb.Position = UDim2.new(1,0,0,3)
        kb.Size = UDim2.fromOffset(70,18)
        kb.BackgroundColor3 = CHECKBG
        kb.BorderSizePixel = 0
        kb.Text = default.Name
        kb.TextColor3 = WHITE
        kb.Font = Enum.Font.Code
        kb.TextSize = 11
        kb.AutoButtonColor = false
        kb.Parent = container
        A.Stroke(kb,1)
        local waiting = false
        kb.MouseButton1Click:Connect(function()
            waiting = true
            kb.Text = "..."
        end)
        UIS.InputBegan:Connect(function(inp,gpe)
            if waiting and not gpe and inp.UserInputType == Enum.UserInputType.Keyboard then
                setter(inp.KeyCode)
                kb.Text = inp.KeyCode.Name
                waiting = false
            end
        end)
        return container
    end

    A.Section = function(parent, title)
        local f = Instance.new("Frame")
        f.Size = UDim2.new(1,0,0,24)
        f.BackgroundColor3 = PANEL
        f.BorderSizePixel = 0
        f.Parent = parent
        A.Stroke(f,1)
        A.MakeText(f,title,UDim2.fromOffset(8,0),UDim2.new(1,-16,1,0),13,ACCENT)
        return f
    end
end

do
    local A = _G.A
    local GUI_PARENT = A.GUI_PARENT
    local WHITE = A.WHITE
    local GRAY = A.GRAY
    local ACCENT = A.ACCENT
    local BG = A.BG
    local UIS = A.UIS
    local Camera = A.Camera

    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "ProjectAstra"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.IgnoreGuiInset = true
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    pcall(function() ScreenGui.Parent = GUI_PARENT end)
    if not ScreenGui.Parent then ScreenGui.Parent = A.LP:WaitForChild("PlayerGui") end

    local Main = Instance.new("Frame")
    Main.Name = "Main"
    Main.Size = UDim2.fromOffset(944,593)
    Main.BackgroundColor3 = BG
    Main.BorderSizePixel = 0
    Main.ClipsDescendants = true
    Main.Parent = ScreenGui

    local MainScale = Instance.new("UIScale")
    MainScale.Scale = 0.55
    MainScale.Parent = Main
    A.Stroke(Main,1.5)

    local function CenterMain()
        local vp = Camera.ViewportSize
        local w = Main.Size.X.Offset * MainScale.Scale
        local h = Main.Size.Y.Offset * MainScale.Scale
        Main.Position = UDim2.fromOffset((vp.X-w)/2,(vp.Y-h)/2)
    end
    CenterMain()
    A.CenterMain = CenterMain

    local TitleBar = Instance.new("Frame")
    TitleBar.Size = UDim2.new(1,0,0,41)
    TitleBar.BackgroundColor3 = Color3.fromRGB(30,20,35)
    TitleBar.BorderSizePixel = 0
    TitleBar.ClipsDescendants = true
    TitleBar.Parent = Main
    local TG = Instance.new("UIGradient", TitleBar)
    TG.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(60,20,80)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(120,30,120)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(40,15,60)),
    })
    A.MakeText(TitleBar, "Project Astra v22.1  |  t.me/epsteinhub", UDim2.fromOffset(12,0), UDim2.new(1,-24,1,0), 14, WHITE)

    local SepLine = Instance.new("Frame")
    SepLine.Position = UDim2.new(0,0,0,41)
    SepLine.Size = UDim2.new(1,0,0,1)
    SepLine.BackgroundColor3 = ACCENT
    SepLine.BorderSizePixel = 0
    SepLine.Parent = Main

    local Tabs = Instance.new("Frame")
    Tabs.Position = UDim2.fromOffset(0,42)
    Tabs.Size = UDim2.new(1,0,0,40)
    Tabs.BackgroundColor3 = BG
    Tabs.BorderSizePixel = 0
    Tabs.Parent = Main

    local TabNames = {"Combat","Visuals","Misc","Settings"}
    local TabButtons = {}
    for i,name in ipairs(TabNames) do
        local b = Instance.new("TextButton")
        b.Position = UDim2.new((i-1)*0.25,0,0,0)
        b.Size = UDim2.new(0.25,0,1,0)
        b.BackgroundTransparency = 1
        b.BorderSizePixel = 0
        b.Text = name
        b.TextColor3 = i==1 and WHITE or GRAY
        b.Font = Enum.Font.Code
        b.TextSize = 14
        b.AutoButtonColor = false
        b.Parent = Tabs
        TabButtons[i] = b
    end

    local SepLine2 = Instance.new("Frame")
    SepLine2.Position = UDim2.new(0,0,0,82)
    SepLine2.Size = UDim2.new(1,0,0,1)
    SepLine2.BackgroundColor3 = ACCENT
    SepLine2.BorderSizePixel = 0
    SepLine2.Parent = Main

    local Pages = {}
    local ScrollsL = {}
    local ScrollsR = {}

    for i,name in ipairs(TabNames) do
        local page = Instance.new("Frame")
        page.Name = name
        page.Position = UDim2.fromOffset(0,83)
        page.Size = UDim2.new(1,0,1,-83)
        page.BackgroundTransparency = 1
        page.BorderSizePixel = 0
        page.Visible = (i==1)
        page.Parent = Main

        local left = Instance.new("ScrollingFrame")
        left.Position = UDim2.new(0,0,0,0)
        left.Size = UDim2.new(0.5,-4,1,0)
        left.BackgroundTransparency = 1
        left.BorderSizePixel = 0
        left.ScrollBarThickness = 3
        left.ScrollBarImageColor3 = ACCENT
        left.AutomaticCanvasSize = Enum.AutomaticSize.Y
        left.CanvasSize = UDim2.new()
        left.Parent = page
        local layoutL = Instance.new("UIListLayout",left)
        layoutL.Padding = UDim.new(0,4)
        layoutL.SortOrder = Enum.SortOrder.LayoutOrder
        local padL = Instance.new("UIPadding",left)
        padL.PaddingTop = UDim.new(0,8)
        padL.PaddingLeft = UDim.new(0,8)
        padL.PaddingRight = UDim.new(0,4)
        padL.PaddingBottom = UDim.new(0,8)

        local right = Instance.new("ScrollingFrame")
        right.Position = UDim2.new(0.5,4,0,0)
        right.Size = UDim2.new(0.5,-4,1,0)
        right.BackgroundTransparency = 1
        right.BorderSizePixel = 0
        right.ScrollBarThickness = 3
        right.ScrollBarImageColor3 = ACCENT
        right.AutomaticCanvasSize = Enum.AutomaticSize.Y
        right.CanvasSize = UDim2.new()
        right.Parent = page
        local layoutR = Instance.new("UIListLayout",right)
        layoutR.Padding = UDim.new(0,4)
        layoutR.SortOrder = Enum.SortOrder.LayoutOrder
        local padR = Instance.new("UIPadding",right)
        padR.PaddingTop = UDim.new(0,8)
        padR.PaddingLeft = UDim.new(0,4)
        padR.PaddingRight = UDim.new(0,8)
        padR.PaddingBottom = UDim.new(0,8)

        Pages[i] = page
        ScrollsL[i] = left
        ScrollsR[i] = right
    end

    local function SelectTab(index)
        for i,page in ipairs(Pages) do page.Visible = (i==index) end
        for i,btn in ipairs(TabButtons) do
            btn.TextColor3 = i==index and WHITE or GRAY
        end
    end
    for i,btn in ipairs(TabButtons) do
        btn.MouseButton1Click:Connect(function() SelectTab(i) end)
    end
    SelectTab(1)

    local Side = Instance.new("Frame")
    Side.Name = "AstraSide"
    Side.Size = UDim2.fromOffset(161,87)
    Side.Position = UDim2.fromOffset(68,185)
    Side.BackgroundColor3 = BG
    Side.BorderSizePixel = 0
    Side.Parent = ScreenGui
    local SideScale = Instance.new("UIScale")
    SideScale.Scale = 0.55
    SideScale.Parent = Side
    A.Stroke(Side,1)

    local ToggleButton = Instance.new("TextButton")
    ToggleButton.Size = UDim2.new(1,0,0,43)
    ToggleButton.BackgroundTransparency = 1
    ToggleButton.BorderSizePixel = 0
    ToggleButton.Text = "Toggle UI"
    ToggleButton.TextColor3 = WHITE
    ToggleButton.Font = Enum.Font.Code
    ToggleButton.TextSize = 14
    ToggleButton.AutoButtonColor = false
    ToggleButton.Parent = Side

    local SideSep = Instance.new("Frame")
    SideSep.Position = UDim2.fromOffset(0,43)
    SideSep.Size = UDim2.new(1,0,0,1)
    SideSep.BackgroundColor3 = ACCENT
    SideSep.BorderSizePixel = 0
    SideSep.Parent = Side

    local LockButton = Instance.new("TextButton")
    LockButton.Position = UDim2.fromOffset(0,44)
    LockButton.Size = UDim2.new(1,0,0,43)
    LockButton.BackgroundTransparency = 1
    LockButton.BorderSizePixel = 0
    LockButton.Text = "Lock UI"
    LockButton.TextColor3 = WHITE
    LockButton.Font = Enum.Font.Code
    LockButton.TextSize = 14
    LockButton.AutoButtonColor = false
    LockButton.Parent = Side

    local MainVisible = true
    local Locked = false
    A.UI_Locked = function() return Locked end

    ToggleButton.MouseButton1Click:Connect(function()
        MainVisible = not MainVisible
        Main.Visible = MainVisible
    end)
    LockButton.MouseButton1Click:Connect(function()
        Locked = not Locked
        LockButton.Text = Locked and "Unlock UI" or "Lock UI"
    end)

    local draggingUI = false
    local dragUIStart, startUIPos
    TitleBar.InputBegan:Connect(function(i)
        if Locked then return end
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            draggingUI = true
            dragUIStart = i.Position
            startUIPos = Main.Position
        end
    end)
    UIS.InputChanged:Connect(function(i)
        if draggingUI and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            local d = i.Position - dragUIStart
            Main.Position = UDim2.fromOffset(startUIPos.X.Offset + d.X, startUIPos.Y.Offset + d.Y)
        end
    end)
    UIS.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            draggingUI = false
        end
    end)

    local sdrag, sdragStart, sStartPos = false, nil, nil
    Side.InputBegan:Connect(function(i)
        if Locked then return end
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            sdrag = true
            sdragStart = i.Position
            sStartPos = Side.Position
        end
    end)
    UIS.InputChanged:Connect(function(i)
        if sdrag and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            local d = i.Position - sdragStart
            Side.Position = UDim2.fromOffset(sStartPos.X.Offset + d.X, sStartPos.Y.Offset + d.Y)
        end
    end)
    UIS.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            sdrag = false
        end
    end)

    A.ScreenGui = ScreenGui
    A.Main = Main
    A.ScrollsL = ScrollsL
    A.ScrollsR = ScrollsR
    A.SelectTab = SelectTab
end

do
    local A = _G.A
    local WHITE = A.WHITE
    local ACCENT = A.ACCENT
    local PANEL = A.PANEL
    local UIS = A.UIS
    local LP = A.LP
    local Camera = A.Camera
    local GUI_PARENT = A.GUI_PARENT
    local Stroke = A.Stroke
    local MakeText = A.MakeText
    local Section = A.Section
    local Checkbox = A.Checkbox
    local Slider = A.Slider
    local Dropdown = A.Dropdown
    local KeybindRow = A.KeybindRow

    local PickerGui = Instance.new("ScreenGui")
    PickerGui.Name = "AstraColorPicker"
    PickerGui.ResetOnSpawn = false
    PickerGui.DisplayOrder = 99999
    pcall(function() PickerGui.Parent = GUI_PARENT end)
    if not PickerGui.Parent then PickerGui.Parent = LP:WaitForChild("PlayerGui") end

    local PickerMain = Instance.new("Frame")
    PickerMain.Size = UDim2.fromOffset(280,260)
    PickerMain.BackgroundColor3 = PANEL
    PickerMain.BorderSizePixel = 0
    PickerMain.Visible = false
    PickerMain.ZIndex = 500
    PickerMain.Parent = PickerGui
    Stroke(PickerMain,1.5)
    MakeText(PickerMain,"Color Picker",UDim2.fromOffset(10,6),UDim2.new(1,-50,0,20),13,WHITE)

    local PickerClose = Instance.new("TextButton")
    PickerClose.Size = UDim2.fromOffset(20,20)
    PickerClose.Position = UDim2.new(1,-26,0,5)
    PickerClose.BackgroundColor3 = Color3.fromRGB(200,40,40)
    PickerClose.BorderSizePixel = 0
    PickerClose.Text = "x"
    PickerClose.TextColor3 = WHITE
    PickerClose.Font = Enum.Font.Code
    PickerClose.TextSize = 11
    PickerClose.AutoButtonColor = false
    PickerClose.Parent = PickerMain
    Instance.new("UICorner",PickerClose).CornerRadius = UDim.new(0,4)
    PickerClose.MouseButton1Click:Connect(function() PickerMain.Visible = false end)

    local Palette = Instance.new("Frame")
    Palette.Position = UDim2.fromOffset(10,34)
    Palette.Size = UDim2.fromOffset(200,150)
    Palette.BackgroundColor3 = Color3.fromHSV(0,1,1)
    Palette.BorderSizePixel = 0
    Palette.ZIndex = 501
    Palette.Parent = PickerMain
    Instance.new("UICorner",Palette).CornerRadius = UDim.new(0,3)

    local WhiteLayer = Instance.new("Frame")
    WhiteLayer.Size = UDim2.fromScale(1,1)
    WhiteLayer.BackgroundColor3 = Color3.new(1,1,1)
    WhiteLayer.BorderSizePixel = 0
    WhiteLayer.ZIndex = 502
    WhiteLayer.Parent = Palette
    local WG = Instance.new("UIGradient",WhiteLayer)
    WG.Transparency = NumberSequence.new({NumberSequenceKeypoint.new(0,0),NumberSequenceKeypoint.new(1,1)})

    local BlackLayer = Instance.new("Frame")
    BlackLayer.Size = UDim2.fromScale(1,1)
    BlackLayer.BackgroundColor3 = Color3.new(0,0,0)
    BlackLayer.BorderSizePixel = 0
    BlackLayer.ZIndex = 503
    BlackLayer.Parent = Palette
    local BG2 = Instance.new("UIGradient",BlackLayer)
    BG2.Rotation = 90
    BG2.Transparency = NumberSequence.new({NumberSequenceKeypoint.new(0,1),NumberSequenceKeypoint.new(1,0)})

    local PaletteCursor = Instance.new("Frame")
    PaletteCursor.AnchorPoint = Vector2.new(0.5,0.5)
    PaletteCursor.Size = UDim2.fromOffset(10,10)
    PaletteCursor.BackgroundTransparency = 1
    PaletteCursor.BorderSizePixel = 0
    PaletteCursor.ZIndex = 510
    PaletteCursor.Parent = Palette
    local PCS = Instance.new("UIStroke",PaletteCursor)
    PCS.Color = Color3.new(1,1,1)
    PCS.Thickness = 2

    local HueBar = Instance.new("Frame")
    HueBar.Position = UDim2.fromOffset(220,34)
    HueBar.Size = UDim2.fromOffset(20,150)
    HueBar.BackgroundColor3 = Color3.new(1,1,1)
    HueBar.BorderSizePixel = 0
    HueBar.ZIndex = 501
    HueBar.Parent = PickerMain
    Instance.new("UICorner",HueBar).CornerRadius = UDim.new(0,4)
    local HG = Instance.new("UIGradient",HueBar)
    HG.Rotation = 90
    HG.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255,0,0)),
        ColorSequenceKeypoint.new(0.166, Color3.fromRGB(255,0,255)),
        ColorSequenceKeypoint.new(0.333, Color3.fromRGB(0,0,255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0,255,255)),
        ColorSequenceKeypoint.new(0.666, Color3.fromRGB(0,255,0)),
        ColorSequenceKeypoint.new(0.833, Color3.fromRGB(255,255,0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255,0,0)),
    })

    local HueCursor = Instance.new("Frame")
    HueCursor.AnchorPoint = Vector2.new(0.5,0.5)
    HueCursor.Size = UDim2.fromOffset(24,4)
    HueCursor.BackgroundColor3 = Color3.new(1,1,1)
    HueCursor.BorderSizePixel = 0
    HueCursor.ZIndex = 520
    HueCursor.Parent = HueBar
    Stroke(HueCursor,1)

    local RGBLabel = MakeText(PickerMain,"RGB: 255,255,255",UDim2.fromOffset(10,190),UDim2.fromOffset(200,18),11,WHITE)

    local ApplyBtn = Instance.new("TextButton")
    ApplyBtn.Position = UDim2.fromOffset(10,214)
    ApplyBtn.Size = UDim2.fromOffset(230,26)
    ApplyBtn.BackgroundColor3 = ACCENT
    ApplyBtn.BorderSizePixel = 0
    ApplyBtn.Text = "Apply"
    ApplyBtn.TextColor3 = WHITE
    ApplyBtn.Font = Enum.Font.Code
    ApplyBtn.TextSize = 12
    ApplyBtn.AutoButtonColor = false
    ApplyBtn.Parent = PickerMain
    Instance.new("UICorner",ApplyBtn).CornerRadius = UDim.new(0,4)
    Stroke(ApplyBtn,1)

    local HSV_h, HSV_s, HSV_v = 0, 1, 1
    local PickColor = Color3.new(1,1,1)
    local PaletteDrag, HueDrag = false, false
    local PickTarget = nil

    local function UpdatePick()
        Palette.BackgroundColor3 = Color3.fromHSV(HSV_h,1,1)
        PickColor = Color3.fromHSV(HSV_h,HSV_s,HSV_v)
        PaletteCursor.Position = UDim2.new(HSV_s,0,1-HSV_v,0)
        HueCursor.Position = UDim2.new(0.5,0,HSV_h,0)
        RGBLabel.Text = string.format("RGB: %d, %d, %d",
            math.floor(PickColor.R*255+0.5),
            math.floor(PickColor.G*255+0.5),
            math.floor(PickColor.B*255+0.5))
    end

    local function UpdatePalettePos(input)
        local pos = Palette.AbsolutePosition
        local size = Palette.AbsoluteSize
        if size.X <= 0 or size.Y <= 0 then return end
        HSV_s = math.clamp((input.Position.X - pos.X)/size.X, 0, 1)
        HSV_v = 1 - math.clamp((input.Position.Y - pos.Y)/size.Y, 0, 1)
        UpdatePick()
    end

    local function UpdateHuePos(input)
        local pos = HueBar.AbsolutePosition
        local size = HueBar.AbsoluteSize
        if size.Y <= 0 then return end
        HSV_h = math.clamp((input.Position.Y - pos.Y)/size.Y, 0, 1)
        UpdatePick()
    end

    Palette.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            PaletteDrag = true
            UpdatePalettePos(i)
        end
    end)
    HueBar.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            HueDrag = true
            UpdateHuePos(i)
        end
    end)
    UIS.InputChanged:Connect(function(i)
        if i.UserInputType ~= Enum.UserInputType.MouseMovement and i.UserInputType ~= Enum.UserInputType.Touch then return end
        if PaletteDrag then UpdatePalettePos(i) end
        if HueDrag then UpdateHuePos(i) end
    end)
    UIS.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            PaletteDrag = false
            HueDrag = false
        end
    end)

    ApplyBtn.MouseButton1Click:Connect(function()
        if PickTarget then PickTarget(PickColor) end
        PickerMain.Visible = false
    end)

    local function OpenColorPicker(startColor, callback)
        local h,s,v = startColor:ToHSV()
        HSV_h, HSV_s, HSV_v = h, s, v
        PickTarget = callback
        UpdatePick()
        local vp = Camera.ViewportSize
        local px = math.clamp(vp.X/2 - 140, 5, vp.X - 290)
        local py = math.clamp(vp.Y/2 - 130, 5, vp.Y - 270)
        PickerMain.Position = UDim2.fromOffset(px, py)
        PickerMain.Visible = true
    end
    A.OpenColorPicker = OpenColorPicker

    local function ColorRow(parent, label, getColor, setColor)
        local container = Instance.new("Frame")
        container.Size = UDim2.new(1,0,0,24)
        container.BackgroundTransparency = 1
        container.Parent = parent
        MakeText(container, label, UDim2.fromOffset(0,0), UDim2.new(1,-60,1,0), 13, WHITE)
        local btn = Instance.new("TextButton")
        btn.AnchorPoint = Vector2.new(1,0)
        btn.Position = UDim2.new(1,0,0,2)
        btn.Size = UDim2.fromOffset(50,20)
        btn.BackgroundColor3 = getColor()
        btn.BorderSizePixel = 0
        btn.Text = ""
        btn.AutoButtonColor = false
        btn.Parent = container
        Stroke(btn,1)
        btn.MouseButton1Click:Connect(function()
            OpenColorPicker(getColor(), function(c)
                btn.BackgroundColor3 = c
                setColor(c)
            end)
        end)
        return container
    end

    local CL = A.ScrollsL[1]
    local CR = A.ScrollsR[1]

    Section(CL, "SILENT AIM")
    Checkbox(CL, "Enable Silent Aim", false, function(v) A.SILENT_AIM = v end)
    Checkbox(CL, "Team Check", false, function(v) A.TEAM_CHECK = v end)
    Checkbox(CL, "Show FOV", true, function(v) A.SHOW_FOV = v end)
    Checkbox(CL, "Snapline", false, function(v) A.SNAPLINE = v end)
    Slider(CL, "Hit Chance", 0, 100, 95, function(v) A.HIT_CHANCE = v end, "%")
    Slider(CL, "FOV Radius", 1, 500, 200, function(v) A.FOV_RADIUS = v end, "px")
    Dropdown(CL, "Hit Part", {"Head","Torso","HumanoidRootPart","Random"}, "Head", function(v) A.HIT_PART = v end)
    ColorRow(CL, "FOV Color", function() return A.silentAimFovColor end, function(c) A.silentAimFovColor = c end)
    ColorRow(CL, "Snapline Color", function() return A.silentAimSnaplineColor end, function(c) A.silentAimSnaplineColor = c end)

    Section(CL, "BULLET TRACER")
    Checkbox(CL, "Enable Tracer", false, function(v) A.TRACER = v end)
    Slider(CL, "Lifetime", 1, 10, 3, function(v) A.TRACER_LIFE = v end, "s")
    Dropdown(CL, "Type", {"Red Laser","Neon Linia Bruh"}, "Red Laser", function(v) A.TRACER_TYPE = v end)

    Section(CR, "RAPID FIRE")
    Checkbox(CR, "Enable Rapid Fire", false, function(v) A.RAPID_FIRE = v end)
    Slider(CR, "Rate Value (x100k)", 1, 100, 100, function(v) A.RAPID_VALUE = v * 100000 end)

    Section(CR, "WEAPON")
    Checkbox(CR, "Instant Hit", false, function(v) A.instantHitEnabled = v end)
    Checkbox(CR, "No Recoil", false, function(v) A.noRecoilEnabled = v end)
    Checkbox(CR, "No Spread", false, function(v) A.noSpreadEnabled = v end)
    Checkbox(CR, "Instant Reload", false, function(v) A.instantReloadEnabled = v end)
    Checkbox(CR, "Auto Reload", false, function(v) A.autoReloadEnabled = v end)
    Checkbox(CR, "No Equip Delay", false, function(v) A.noEquipDelayEnabled = v end)
    Checkbox(CR, "Full Auto", false, function(v) A.fullAutoEnabled = v end)
    Slider(CR, "Bullet Speed", 500, 5000, 5000, function(v) A.bulletSpeedValue = v end)

    Section(CR, "HIT MARKER")
    Checkbox(CR, "Enable Hit Marker", true, function(v) A.hitMarkerEnabled = v end)
    ColorRow(CR, "Marker Color", function() return A.hitMarkerColor end, function(c) A.hitMarkerColor = c end)
    Slider(CR, "Marker Size", 4, 30, 9, function(v) A.hitMarkerSize = v end, "px")
    Slider(CR, "Duration (x10ms)", 1, 100, 35, function(v) A.hitMarkerDuration = v/100 end, "s")

    Section(CR, "HIT SOUND")
    Checkbox(CR, "Enable Hit Sound", false, function(v) A.HIT_SOUND = v end)
    Dropdown(CR, "Sound Preset", {"Rust","Minecraft","Rthro","Minecraft bow","Skeet","Neverlose","Untimity"}, "Rust",
        function(v)
            local p = A.HIT_SOUND_PRESETS[v]
            if p then A.HIT_SOUND_ID = p end
        end)
    Slider(CR, "Volume (x0.01)", 0, 200, 30, function(v) A.HIT_SOUND_VOL = v/100 end)

    local VL = A.ScrollsL[2]
    local VR = A.ScrollsR[2]

    Section(VL, "ESP")
    Checkbox(VL, "Enable ESP", false, function(v) A.espEnabled = v end)
    Checkbox(VL, "Box", true, function(v) A.espBox = v end)
    ColorRow(VL, "Box Color", function() return A.espBoxColor end, function(c) A.espBoxColor = c end)
    Checkbox(VL, "Name", true, function(v) A.espName = v end)
    ColorRow(VL, "Name Color", function() return A.espNameColor end, function(c) A.espNameColor = c end)
    Checkbox(VL, "Health", true, function(v) A.espHealth = v end)
    Checkbox(VL, "Distance", true, function(v) A.espDistance = v end)
    ColorRow(VL, "Dist Color", function() return A.espDistColor end, function(c) A.espDistColor = c end)
    Checkbox(VL, "Weapon", true, function(v) A.espWeapon = v end)
    ColorRow(VL, "Weapon Color", function() return A.espWeapColor end, function(c) A.espWeapColor = c end)

    Section(VL, "CUSTOM HANDS")
    Checkbox(VL, "Enable Custom Hands", false, function(v)
        A.customHandsEnabled = v
        if v and A.SetupCustomHands then A.SetupCustomHands() end
        if not v then A.activeLockedArms = {} end
    end)
    ColorRow(VL, "Hands Color", function() return A.customHandsColor end, function(c)
        A.customHandsColor = c
        for part,_ in pairs(A.activeLockedArms) do
            pcall(function() part.Color = c end)
        end
    end)
    Slider(VL, "Hands Transparency", 0, 100, 55, function(v) A.customHandsTransp = v/100 end, "%")

    Section(VL, "VIEW MODEL")
    Checkbox(VL, "Enable View Model", false, function(v)
        A.viewModelEnabled = v
        if v and A.ApplyViewModel then A.ApplyViewModel() end
        if not v and A.RemoveViewModel then A.RemoveViewModel() end
    end)
    Slider(VL, "X", -20, 20, 5, function(v) A.viewModelX = v end)
    Slider(VL, "Y", -20, 20, -2, function(v) A.viewModelY = v end)
    Slider(VL, "Z", -20, 20, -8, function(v) A.viewModelZ = v end)

    Section(VR, "GUN CHAMS")
    Checkbox(VR, "Enable Gun Chams", false, function(v) A.gunChamsEnabled = v end)
    ColorRow(VR, "Gun Chams Color", function() return A.gunChamsColor end, function(c) A.gunChamsColor = c end)

    Section(VR, "GAMMA")
    Checkbox(VR, "Enable Gamma", false, function(v)
        A.gammaEnabled = v
        local L = A.Lighting
        if v then
            L.Ambient = A.gammaColor
            L.OutdoorAmbient = A.gammaColor
            L.Brightness = A.gammaValue/10
        else
            L.Ambient = Color3.fromRGB(128,128,128)
            L.OutdoorAmbient = Color3.fromRGB(128,128,128)
            L.Brightness = 1
        end
    end)
    ColorRow(VR, "Gamma Color", function() return A.gammaColor end, function(c)
        A.gammaColor = c
        if A.gammaEnabled then
            A.Lighting.Ambient = c
            A.Lighting.OutdoorAmbient = c
        end
    end)
    Slider(VR, "Gamma Brightness (x0.1)", 1, 50, 10, function(v)
        A.gammaValue = v
        if A.gammaEnabled then A.Lighting.Brightness = v/10 end
    end)

    Section(VR, "FOG")
    Checkbox(VR, "Enable Fog", false, function(v)
        A.fogEnabled = v
        if v then
            A.Lighting.FogStart = A.fogStart
            A.Lighting.FogEnd = A.fogEnd
            A.Lighting.FogColor = A.fogColor
        else
            A.Lighting.FogEnd = 100000
            A.Lighting.FogStart = 0
        end
    end)
    ColorRow(VR, "Fog Color", function() return A.fogColor end, function(c)
        A.fogColor = c
        if A.fogEnabled then A.Lighting.FogColor = c end
    end)
    Slider(VR, "Fog Start", 0, 2000, 100, function(v)
        A.fogStart = v
        if A.fogEnabled then A.Lighting.FogStart = v end
    end)
    Slider(VR, "Fog End", 50, 5000, 500, function(v)
        A.fogEnd = v
        if A.fogEnabled then A.Lighting.FogEnd = v end
    end)

    Section(VR, "SKYBOX")
    Checkbox(VR, "Enable Skybox", false, function(v)
        A.skyboxEnabled = v
        if not v and A.activeSky then A.activeSky:Destroy() A.activeSky = nil end
        if v and A.ApplySkybox then A.ApplySkybox(A.skyboxMode) end
    end)
    Dropdown(VR, "Skybox Preset",
        {"None","Aurorka","Galaxy","Nebula","Nebula 2","Orion"},
        "None",
        function(v)
            A.skyboxMode = v
            if A.skyboxEnabled and A.ApplySkybox then A.ApplySkybox(v) end
        end)

    Section(VR, "CAMERA / WORLD")
    Checkbox(VR, "Custom FOV", false, function(v) A.cameraFovEnabled = v end)
    Slider(VR, "FOV Value", 60, 160, 120, function(v) A.cameraFovValue = v end)
    Checkbox(VR, "No Fog", false, function(v)
        A.noFogEnabled = v
        if v then
            A.Lighting.FogEnd = 1e6
            A.Lighting.FogStart = 1e5
        else
            A.Lighting.FogEnd = 100000
            A.Lighting.FogStart = 0
        end
    end)
    Checkbox(VR, "Fullbright", false, function(v)
        A.fullbrightEnabled = v
        if v then
            A.Lighting.Brightness = 3
            A.Lighting.ClockTime = 14
            A.Lighting.GlobalShadows = false
            A.Lighting.FogEnd = 1e9
        else
            A.Lighting.Brightness = 1
            A.Lighting.GlobalShadows = true
            A.Lighting.FogEnd = 100000
        end
    end)

    local ML = A.ScrollsL[3]
    local MR = A.ScrollsR[3]

    Section(ML, "MOVEMENT")
    Checkbox(ML, "Speed Hack", false, function(v)
        A.speedHackEnabled = v
        local h = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if h then h.WalkSpeed = v and A.speedValue or 16 end
    end)
    Slider(ML, "Speed Value", 16, 500, 50, function(v)
        A.speedValue = v
        if A.speedHackEnabled then
            local h = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
            if h then h.WalkSpeed = v end
        end
    end)
    Checkbox(ML, "Infinite Jump", false, function(v) A.infJumpEnabled = v end)
    Checkbox(ML, "No Clip", false, function(v) A.noClipEnabled = v end)
    Checkbox(ML, "Bunny Hop", false, function(v) A.bhopEnabled = v end)
    Checkbox(ML, "Jump Shot", false, function(v) A.JUMP_SHOT = v end)

    Section(ML, "FLY")
    Checkbox(ML, "Enable Fly", false, function(v) A.flyEnabled = v end)
    Slider(ML, "Fly Speed", 5, 500, 50, function(v) A.flySpeed = v end)
    KeybindRow(ML, "Fly Keybind", A.keybinds.Fly, function(k) A.keybinds.Fly = k end)

    Section(MR, "TELEPORT")
    Checkbox(MR, "Show TP Button (mobile)", false, function(v)
        if v then
            if A.CreateTPButton then A.CreateTPButton() end
        else
            if A.DestroyTPButton then A.DestroyTPButton() end
        end
    end)
    KeybindRow(MR, "TP Keybind", A.tpKey, function(k) A.tpKey = k end)
    Slider(MR, "TP Distance", 10, 500, 100, function(v) A.tpDistance = v end)

    Section(MR, "FREEZE")
    Checkbox(MR, "Enable Freeze (hold key)", false, function(v) A.freezeEnabled = v end)
    KeybindRow(MR, "Freeze Keybind", A.keybinds.Freeze, function(k) A.keybinds.Freeze = k end)
    Checkbox(MR, "Show Freeze Button (mobile)", false, function(v)
        if v then
            if A.CreateFreezeButton then A.CreateFreezeButton() end
        else
            if A.DestroyFreezeButton then A.DestroyFreezeButton() end
        end
    end)

    Section(MR, "AIR WALK")
    KeybindRow(MR, "Air Walk Keybind", A.airWalkKey, function(k) A.airWalkKey = k end)
    Slider(MR, "Air Walk Duration", 1, 10, 3, function(v) A.airWalkDuration = v end, "s")
    Slider(MR, "Air Walk Height", 20, 300, 100, function(v) A.airWalkHeight = v end)
    Checkbox(MR, "Show Air Walk Button (mobile)", false, function(v)
        if v then
            if A.CreateAirWalkButton then A.CreateAirWalkButton() end
        else
            if A.DestroyAirWalkButton then A.DestroyAirWalkButton() end
        end
    end)

    local SL = A.ScrollsL[4]
    local SR = A.ScrollsR[4]

    Section(SL, "INFO")
    MakeText(SL, "Project Astra v22.1", UDim2.fromOffset(10,0), UDim2.new(1,-20,0,22), 13, ACCENT)
    MakeText(SL, "t.me/epsteinhub", UDim2.fromOffset(10,24), UDim2.new(1,-20,0,22), 13, WHITE)

    Section(SL, "UI SETTINGS")
    Checkbox(SL, "Show Keybind Overlay", false, function(v)
        A.showKeybindOverlay = v
        if A.SetKeybindOverlayVisible then A.SetKeybindOverlayVisible(v) end
    end)

    Section(SL, "WHITELIST")
    local wlFrame = Instance.new("ScrollingFrame")
    wlFrame.Size = UDim2.new(1,-20,0,240)
    wlFrame.Position = UDim2.fromOffset(10,0)
    wlFrame.BackgroundColor3 = PANEL
    wlFrame.BorderSizePixel = 0
    wlFrame.ScrollBarThickness = 3
    wlFrame.ScrollBarImageColor3 = ACCENT
    wlFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
    wlFrame.CanvasSize = UDim2.new()
    wlFrame.Parent = SL
    Stroke(wlFrame,1)
    Instance.new("UIListLayout",wlFrame).Padding = UDim.new(0,3)

    local function RenderWhitelist()
        for _, c in ipairs(wlFrame:GetChildren()) do
            if c:IsA("TextButton") then c:Destroy() end
        end
        for _, p in ipairs(A.Players:GetPlayers()) do
            if p ~= LP then
                local b = Instance.new("TextButton")
                b.Size = UDim2.new(1,-6,0,24)
                b.BackgroundColor3 = A.whitelistPlayers[p.Name] and Color3.fromRGB(120,30,120) or Color3.fromRGB(20,20,20)
                b.BorderSizePixel = 0
                b.Text = p.Name .. (A.whitelistPlayers[p.Name] and "  [WL]" or "")
                b.TextColor3 = WHITE
                b.Font = Enum.Font.Code
                b.TextSize = 12
                b.TextXAlignment = Enum.TextXAlignment.Left
                b.AutoButtonColor = false
                b.Parent = wlFrame
                Instance.new("UIPadding",b).PaddingLeft = UDim.new(0,6)
                b.MouseButton1Click:Connect(function()
                    A.whitelistPlayers[p.Name] = not A.whitelistPlayers[p.Name] or nil
                    RenderWhitelist()
                end)
            end
        end
    end
    A.RenderWhitelist = RenderWhitelist
    A.Players.PlayerAdded:Connect(RenderWhitelist)
    A.Players.PlayerRemoving:Connect(RenderWhitelist)
    RenderWhitelist()

    local HAS_FS = (type(writefile) == "function" and type(readfile) == "function" and type(isfile) == "function")
    local HAS_LIST = (type(listfiles) == "function")
    local HAS_FOLDER = (type(makefolder) == "function" and type(isfolder) == "function")

    Section(SR, "CONFIGS")

    if not HAS_FS then
        MakeText(SR, "Executor без файловой системы", UDim2.fromOffset(10,0), UDim2.new(1,-20,0,22), 12, Color3.fromRGB(220,80,80))
    else
        local nameBox = Instance.new("TextBox")
        nameBox.Size = UDim2.new(1,-20,0,28)
        nameBox.Position = UDim2.fromOffset(10,0)
        nameBox.BackgroundColor3 = A.DROPBG
        nameBox.BorderSizePixel = 0
        nameBox.Text = "my_config"
        nameBox.TextColor3 = WHITE
        nameBox.Font = Enum.Font.Code
        nameBox.TextSize = 12
        nameBox.ClearTextOnFocus = false
        nameBox.Parent = SR
        Stroke(nameBox,1)

        local saveBtn = Instance.new("TextButton")
        saveBtn.Size = UDim2.new(1,-20,0,26)
        saveBtn.Position = UDim2.fromOffset(10,32)
        saveBtn.BackgroundColor3 = Color3.fromRGB(60,120,60)
        saveBtn.BorderSizePixel = 0
        saveBtn.Text = "SAVE"
        saveBtn.TextColor3 = WHITE
        saveBtn.Font = Enum.Font.Code
        saveBtn.TextSize = 12
        saveBtn.AutoButtonColor = false
        saveBtn.Parent = SR
        Stroke(saveBtn,1)

        local AutoLoadVal = false
        local autoRow = Instance.new("Frame")
        autoRow.Size = UDim2.new(1,-20,0,24)
        autoRow.Position = UDim2.fromOffset(10,62)
        autoRow.BackgroundTransparency = 1
        autoRow.Parent = SR
        MakeText(autoRow, "Auto-Load on Inject", UDim2.fromOffset(26,0), UDim2.new(1,-26,1,0), 13, WHITE)
        local autoBox = Instance.new("TextButton")
        autoBox.Size = UDim2.fromOffset(18,18)
        autoBox.Position = UDim2.fromOffset(0,3)
        autoBox.BackgroundColor3 = A.CHECKBG
        autoBox.BorderSizePixel = 0
        autoBox.Text = ""
        autoBox.AutoButtonColor = false
        autoBox.Parent = autoRow
        Stroke(autoBox,1)
        local autoFill = Instance.new("Frame")
        autoFill.Position = UDim2.fromOffset(3,3)
        autoFill.Size = UDim2.fromOffset(12,12)
        autoFill.BackgroundColor3 = ACCENT
        autoFill.BorderSizePixel = 0
        autoFill.Visible = false
        autoFill.Parent = autoBox
        autoBox.MouseButton1Click:Connect(function()
            AutoLoadVal = not AutoLoadVal
            autoFill.Visible = AutoLoadVal
        end)

        local cfgList = Instance.new("ScrollingFrame")
        cfgList.Size = UDim2.new(1,-20,0,200)
        cfgList.Position = UDim2.fromOffset(10,92)
        cfgList.BackgroundColor3 = PANEL
        cfgList.BorderSizePixel = 0
        cfgList.ScrollBarThickness = 3
        cfgList.ScrollBarImageColor3 = ACCENT
        cfgList.AutomaticCanvasSize = Enum.AutomaticSize.Y
        cfgList.CanvasSize = UDim2.new()
        cfgList.Parent = SR
        Stroke(cfgList,1)
        Instance.new("UIListLayout",cfgList).Padding = UDim.new(0,3)

        local function GetPath(n)
            return A.CONFIG_FOLDER .. "/" .. n .. ".json"
        end

        local function EnsureFolder()
            if HAS_FOLDER then
                pcall(function()
                    if not isfolder(A.CONFIG_FOLDER) then makefolder(A.CONFIG_FOLDER) end
                end)
            end
        end

        local function CollectConfig()
            return {
                SILENT_AIM = A.SILENT_AIM, HIT_CHANCE = A.HIT_CHANCE, HIT_PART = A.HIT_PART,
                FOV_RADIUS = A.FOV_RADIUS, SHOW_FOV = A.SHOW_FOV, TEAM_CHECK = A.TEAM_CHECK,
                SNAPLINE = A.SNAPLINE, TRACER = A.TRACER, TRACER_TYPE = A.TRACER_TYPE, TRACER_LIFE = A.TRACER_LIFE,
                RAPID_FIRE = A.RAPID_FIRE, RAPID_VALUE = A.RAPID_VALUE,
                HIT_SOUND = A.HIT_SOUND, HIT_SOUND_ID = A.HIT_SOUND_ID, HIT_SOUND_VOL = A.HIT_SOUND_VOL,
                hitMarkerEnabled = A.hitMarkerEnabled, hitMarkerSize = A.hitMarkerSize, hitMarkerDuration = A.hitMarkerDuration,
                instantHitEnabled = A.instantHitEnabled, noRecoilEnabled = A.noRecoilEnabled, noSpreadEnabled = A.noSpreadEnabled,
                instantReloadEnabled = A.instantReloadEnabled, autoReloadEnabled = A.autoReloadEnabled,
                noEquipDelayEnabled = A.noEquipDelayEnabled, fullAutoEnabled = A.fullAutoEnabled, bulletSpeedValue = A.bulletSpeedValue,
                cameraFovEnabled = A.cameraFovEnabled, cameraFovValue = A.cameraFovValue,
                noFogEnabled = A.noFogEnabled, fullbrightEnabled = A.fullbrightEnabled,
                espEnabled = A.espEnabled, espBox = A.espBox, espName = A.espName, espHealth = A.espHealth,
                espDistance = A.espDistance, espWeapon = A.espWeapon,
                customHandsEnabled = A.customHandsEnabled, customHandsTransp = A.customHandsTransp,
                viewModelEnabled = A.viewModelEnabled, viewModelX = A.viewModelX, viewModelY = A.viewModelY, viewModelZ = A.viewModelZ,
                gunChamsEnabled = A.gunChamsEnabled, skyboxEnabled = A.skyboxEnabled, skyboxMode = A.skyboxMode,
                gammaEnabled = A.gammaEnabled, gammaValue = A.gammaValue,
                fogEnabled = A.fogEnabled, fogStart = A.fogStart, fogEnd = A.fogEnd,
                speedHackEnabled = A.speedHackEnabled, speedValue = A.speedValue,
                infJumpEnabled = A.infJumpEnabled, noClipEnabled = A.noClipEnabled, bhopEnabled = A.bhopEnabled,
                JUMP_SHOT = A.JUMP_SHOT, flyEnabled = A.flyEnabled, flySpeed = A.flySpeed,
                whitelistEnabled = A.whitelistEnabled, whitelistPlayers = A.whitelistPlayers,
                silentAimFovColor = {A.silentAimFovColor.R, A.silentAimFovColor.G, A.silentAimFovColor.B},
                silentAimSnaplineColor = {A.silentAimSnaplineColor.R, A.silentAimSnaplineColor.G, A.silentAimSnaplineColor.B},
                hitMarkerColor = {A.hitMarkerColor.R, A.hitMarkerColor.G, A.hitMarkerColor.B},
                espBoxColor = {A.espBoxColor.R, A.espBoxColor.G, A.espBoxColor.B},
                espNameColor = {A.espNameColor.R, A.espNameColor.G, A.espNameColor.B},
                espDistColor = {A.espDistColor.R, A.espDistColor.G, A.espDistColor.B},
                espWeapColor = {A.espWeapColor.R, A.espWeapColor.G, A.espWeapColor.B},
                customHandsColor = {A.customHandsColor.R, A.customHandsColor.G, A.customHandsColor.B},
                gunChamsColor = {A.gunChamsColor.R, A.gunChamsColor.G, A.gunChamsColor.B},
                gammaColor = {A.gammaColor.R, A.gammaColor.G, A.gammaColor.B},
                fogColor = {A.fogColor.R, A.fogColor.G, A.fogColor.B},
                AutoLoad = AutoLoadVal,
            }
        end

        local colorKeys = {
            silentAimFovColor=true, silentAimSnaplineColor=true, hitMarkerColor=true,
            espBoxColor=true, espNameColor=true, espDistColor=true, espWeapColor=true,
            customHandsColor=true, gunChamsColor=true, gammaColor=true, fogColor=true,
        }

        local function ApplyConfig(cfg)
            if not cfg then return end
            for k, v in pairs(cfg) do
                if colorKeys[k] and type(v) == "table" and #v == 3 then
                    A[k] = Color3.new(v[1], v[2], v[3])
                elseif k ~= "AutoLoad" then
                    A[k] = v
                end
            end
            RenderWhitelist()
        end

        local function RenderCfgList()
            for _, c in ipairs(cfgList:GetChildren()) do
                if c:IsA("Frame") or c:IsA("TextLabel") then c:Destroy() end
            end
            if not HAS_LIST then return end
            EnsureFolder()
            local ok, files = pcall(listfiles, A.CONFIG_FOLDER)
            if not ok or not files then return end
            for _, f in ipairs(files) do
                local n = f:match("([^/\\]+)%.json$")
                if n and n ~= "autoload" then
                    local row = Instance.new("Frame")
                    row.Size = UDim2.new(1,-6,0,26)
                    row.BackgroundColor3 = Color3.fromRGB(20,20,20)
                    row.BorderSizePixel = 0
                    row.Parent = cfgList
                    Instance.new("UICorner",row).CornerRadius = UDim.new(0,3)
                    local lbl = Instance.new("TextLabel")
                    lbl.Size = UDim2.new(1,-76,1,0)
                    lbl.BackgroundTransparency = 1
                    lbl.Text = "  " .. n
                    lbl.TextColor3 = WHITE
                    lbl.Font = Enum.Font.Code
                    lbl.TextSize = 12
                    lbl.TextXAlignment = Enum.TextXAlignment.Left
                    lbl.Parent = row
                    local lb = Instance.new("TextButton")
                    lb.Size = UDim2.fromOffset(34,20)
                    lb.Position = UDim2.new(1,-72,0.5,-10)
                    lb.BackgroundColor3 = Color3.fromRGB(60,120,60)
                    lb.BorderSizePixel = 0
                    lb.Text = "LOAD"
                    lb.TextColor3 = WHITE
                    lb.Font = Enum.Font.Code
                    lb.TextSize = 9
                    lb.AutoButtonColor = false
                    lb.Parent = row
                    Instance.new("UICorner",lb).CornerRadius = UDim.new(0,3)
                    Stroke(lb,1)
                    local db = Instance.new("TextButton")
                    db.Size = UDim2.fromOffset(34,20)
                    db.Position = UDim2.new(1,-34,0.5,-10)
                    db.BackgroundColor3 = Color3.fromRGB(150,40,40)
                    db.BorderSizePixel = 0
                    db.Text = "DEL"
                    db.TextColor3 = WHITE
                    db.Font = Enum.Font.Code
                    db.TextSize = 9
                    db.AutoButtonColor = false
                    db.Parent = row
                    Instance.new("UICorner",db).CornerRadius = UDim.new(0,3)
                    Stroke(db,1)
                    lb.MouseButton1Click:Connect(function()
                        pcall(function()
                            local path = GetPath(n)
                            if isfile(path) then
                                local data = A.HttpService:JSONDecode(readfile(path))
                                ApplyConfig(data)
                                nameBox.Text = n
                                if data.AutoLoad ~= nil then
                                    AutoLoadVal = data.AutoLoad
                                    autoFill.Visible = AutoLoadVal
                                end
                            end
                        end)
                    end)
                    db.MouseButton1Click:Connect(function()
                        pcall(function()
                            local path = GetPath(n)
                            if type(delfile) == "function" and isfile(path) then
                                delfile(path)
                                RenderCfgList()
                            end
                        end)
                    end)
                end
            end
        end

        saveBtn.MouseButton1Click:Connect(function()
            pcall(function()
                local n = nameBox.Text
                if n == "" then n = "default" end
                EnsureFolder()
                writefile(GetPath(n), A.HttpService:JSONEncode(CollectConfig()))
                RenderCfgList()
                if AutoLoadVal then
                    writefile(GetPath("autoload"), A.HttpService:JSONEncode(CollectConfig()))
                end
            end)
        end)

        RenderCfgList()

        pcall(function()
            if isfile(GetPath("autoload")) then
                local data = A.HttpService:JSONDecode(readfile(GetPath("autoload")))
                ApplyConfig(data)
            end
        end)
    end
end

do
    local A = _G.A
    local UIS = A.UIS
    local RS = A.RS
    local LP = A.LP
    local Camera = A.Camera
    local Players = A.Players
    local Lighting = A.Lighting
    local Debris = A.Debris
    local GUI_PARENT = A.GUI_PARENT

    local FOVGui = Instance.new("ScreenGui")
    FOVGui.Name = "AstraFOV"
    FOVGui.ResetOnSpawn = false
    FOVGui.IgnoreGuiInset = true
    pcall(function() FOVGui.Parent = GUI_PARENT end)
    if not FOVGui.Parent then FOVGui.Parent = LP:WaitForChild("PlayerGui") end

    local FOVFrame = Instance.new("Frame")
    FOVFrame.BackgroundTransparency = 1
    FOVFrame.Visible = false
    FOVFrame.ZIndex = 999
    FOVFrame.Parent = FOVGui
    local FS = Instance.new("UIStroke",FOVFrame)
    FS.Thickness = 1.5
    FS.Transparency = 0.3
    Instance.new("UICorner",FOVFrame).CornerRadius = UDim.new(1,0)

    local snapLine = nil
    if Drawing then
        snapLine = Drawing.new("Line")
        snapLine.Visible = false
        snapLine.Thickness = 2
        snapLine.Transparency = 0.8
        snapLine.Color = A.silentAimSnaplineColor
    end

    function A.getSilentTarget()
        local best, bd = nil, A.FOV_RADIUS
        local vp = Camera.ViewportSize
        local center = Vector2.new(vp.X/2, vp.Y/2)
        for _,p in ipairs(Players:GetPlayers()) do
            if p == LP then continue end
            if A.TEAM_CHECK and p.Team == LP.Team then continue end
            if A.whitelistEnabled and A.whitelistPlayers[p.Name] then continue end
            local char = p.Character
            if not char then continue end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum or hum.Health <= 0 then continue end
            local partName = A.HIT_PART
            if partName == "Random" then
                local opts = {"Head","Torso","HumanoidRootPart"}
                partName = opts[math.random(1,#opts)]
            end
            local part = char:FindFirstChild(partName) or char:FindFirstChild("Head")
            if not part then continue end
            local pos, onS = Camera:WorldToViewportPoint(part.Position)
            if not onS then continue end
            local dist = (Vector2.new(pos.X, pos.Y) - center).Magnitude
            if dist < bd then bd = dist best = part end
        end
        return best
    end

    function A.CreateHitmarker(worldPosition)
        if not A.hitMarkerEnabled then return end
        if not Drawing then return end
        local markerId = tostring(math.random(1,100000))
        local markerData = {
            WorldPosition = worldPosition,
            Size = A.hitMarkerSize,
            Gap = A.hitMarkerGap,
            Opacity = 1,
            Lines = {}
        }
        for i = 1,4 do
            local line = Drawing.new("Line")
            line.Visible = false
            line.Color = A.hitMarkerColor
            line.Thickness = 2.5
            table.insert(markerData.Lines, line)
        end
        A.ActiveHitmarkers[markerId] = markerData
        task.spawn(function()
            local duration = math.max(0.05, A.hitMarkerDuration)
            local totalSteps = 20
            local stepTime = duration / totalSteps
            for i = totalSteps, 0, -1 do
                if not A.ActiveHitmarkers[markerId] then return end
                markerData.Opacity = i / totalSteps
                task.wait(stepTime)
            end
            for _,line in ipairs(markerData.Lines) do pcall(function() line:Remove() end) end
            A.ActiveHitmarkers[markerId] = nil
        end)
    end

    function A.CreateTracer(from, to)
        if not A.TRACER then return end
        if (to - from).Magnitude < 1 then return end
        local holder = Instance.new("Part")
        holder.Size = Vector3.new(0.05,0.05,0.05)
        holder.Transparency = 1
        holder.Anchored = true
        holder.CanCollide = false
        holder.Position = from
        holder.Parent = workspace
        local a0 = Instance.new("Attachment", holder)
        a0.WorldPosition = from
        local a1 = Instance.new("Attachment", holder)
        a1.WorldPosition = to
        local beam = Instance.new("Beam")
        beam.Attachment0 = a0
        beam.Attachment1 = a1
        beam.FaceCamera = true
        beam.LightEmission = 1
        if A.TRACER_TYPE == "Red Laser" then
            beam.Width0 = 1.2
            beam.Width1 = 1.2
            beam.Color = ColorSequence.new(Color3.fromRGB(255,10,10))
        else
            beam.Width0 = 0.4
            beam.Width1 = 0.4
            beam.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(255,255,255)),
                ColorSequenceKeypoint.new(0.5, Color3.fromRGB(150,50,220)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(255,255,255)),
            })
        end
        beam.Parent = holder
        Debris:AddItem(holder, A.TRACER_LIFE)
    end

    local function createESP(player)
        if A.espCache[player] then return end
        if not Drawing then return end
        local o = {
            Box = Drawing.new("Square"),
            Name = Drawing.new("Text"),
            HPBar = Drawing.new("Square"),
            HPBg = Drawing.new("Square"),
            Dist = Drawing.new("Text"),
            Weapon = Drawing.new("Text"),
        }
        o.Box.Filled = false
        o.Box.Thickness = 1.5
        o.Name.Size = 14
        o.Name.Center = true
        o.Name.Outline = true
        o.HPBg.Filled = true
        o.HPBg.Color = Color3.fromRGB(40,40,40)
        o.HPBar.Filled = true
        o.Dist.Size = 12
        o.Dist.Center = true
        o.Dist.Outline = true
        o.Weapon.Size = 12
        o.Weapon.Center = true
        o.Weapon.Outline = true
        for _,d in pairs(o) do d.Visible = false end
        A.espCache[player] = o
    end

    local function removeESP(player)
        if A.espCache[player] then
            for _,d in pairs(A.espCache[player]) do pcall(function() d:Remove() end) end
            A.espCache[player] = nil
        end
    end

    Players.PlayerAdded:Connect(function(p) if p ~= LP then createESP(p) end end)
    Players.PlayerRemoving:Connect(removeESP)
    for _,p in ipairs(Players:GetPlayers()) do if p ~= LP then createESP(p) end end

    function A.ApplySkybox(name)
        if A.activeSky then A.activeSky:Destroy() A.activeSky = nil end
        if name == "None" then return end
        for _,o in ipairs(Lighting:GetChildren()) do
            if o:IsA("Sky") or o:IsA("Atmosphere") then o:Destroy() end
        end
        local s = Instance.new("Sky")
        if name == "Galaxy" then
            s.SkyboxFt = "rbxassetid://99674108178510"
            s.SkyboxBk = "rbxassetid://76898653037615"
            s.SkyboxLf = "rbxassetid://78775818685659"
            s.SkyboxRt = "rbxassetid://117864739442861"
            s.SkyboxUp = "rbxassetid://94326041205929"
            s.SkyboxDn = "rbxassetid://134726625576772"
        else
            local id = A.SKYBOXES[name]
            if id and id ~= "MULTI" then
                s.SkyboxBk = id
                s.SkyboxDn = id
                s.SkyboxFt = id
                s.SkyboxLf = id
                s.SkyboxRt = id
                s.SkyboxUp = id
            end
        end
        s.Parent = Lighting
        A.activeSky = s
    end

    local TARGET_ARM_NAMES = {["RightArm"]=true,["LeftArm"]=true,["SleeveL"]=true,["SleeveR"]=true}

    local function BindArm(part)
        if A.activeLockedArms[part] then return end
        A.activeLockedArms[part] = true
        part.Color = A.customHandsColor
        part.Transparency = A.customHandsTransp
        part.Material = Enum.Material.Neon
    end

    function A.SetupCustomHands()
        for _,obj in ipairs(game:GetDescendants()) do
            if obj:IsA("BasePart") and TARGET_ARM_NAMES[obj.Name] then BindArm(obj) end
        end
    end

    function A.ApplyViewModel()
        if A.viewModelConn then A.viewModelConn:Disconnect() A.viewModelConn = nil end
        if not A.viewModelEnabled then return end
        A.viewModelConn = RS.RenderStepped:Connect(function()
            if not A.viewModelEnabled then return end
            local char = LP.Character
            if not char then return end
            local tool = char:FindFirstChildOfClass("Tool")
            if not tool then return end
            local toolWorld = workspace:FindFirstChild(tool.Name)
            if not toolWorld then return end
            pcall(function()
                toolWorld:PivotTo(Camera.CFrame * CFrame.new(A.viewModelX, A.viewModelY, A.viewModelZ))
            end)
        end)
    end

    function A.RemoveViewModel()
        if A.viewModelConn then A.viewModelConn:Disconnect() A.viewModelConn = nil end
    end

    function A.DoTeleport()
        local char = LP.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if not root then return end
        root.CFrame = CFrame.new(root.Position + Camera.CFrame.LookVector * A.tpDistance) * root.CFrame.Rotation
        root.Velocity = Vector3.zero
    end

    function A.DoAirWalk()
        if A.airWalkBusy then return end
        local char = LP.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if not root or not hum then return end
        A.airWalkBusy = true
        local targetY = root.Position.Y + A.airWalkHeight
        root.CFrame = CFrame.new(root.Position.X, targetY, root.Position.Z) * root.CFrame.Rotation
        local bp = Instance.new("BodyPosition")
        bp.MaxForce = Vector3.new(0, math.huge, 0)
        bp.Position = Vector3.new(0, targetY, 0)
        bp.P = 25000
        bp.Parent = root
        hum:ChangeState(Enum.HumanoidStateType.Running)
        task.wait(A.airWalkDuration)
        if bp then bp:Destroy() end
        root.Velocity = Vector3.zero
        hum:ChangeState(Enum.HumanoidStateType.GettingUp)
        A.airWalkBusy = false
    end

    local function SetupDraggable(btn)
        local dragging, dragInput, dragStart, startPos
        btn.InputBegan:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                dragging = true
                dragStart = i.Position
                startPos = btn.Position
                i.Changed:Connect(function()
                    if i.UserInputState == Enum.UserInputState.End then dragging = false end
                end)
            end
        end)
        btn.InputChanged:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch then
                dragInput = i
            end
        end)
        UIS.InputChanged:Connect(function(i)
            if i == dragInput and dragging then
                local d = i.Position - dragStart
                btn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + d.X, startPos.Y.Scale, startPos.Y.Offset + d.Y)
            end
        end)
    end

    function A.CreateTPButton()
        if A.tpBtnGui then return end
        local g = Instance.new("ScreenGui")
        g.Name = "AstraTPButton"
        g.ResetOnSpawn = false
        pcall(function() g.Parent = GUI_PARENT end)
        if not g.Parent then g.Parent = LP:WaitForChild("PlayerGui") end
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0,110,0,45)
        btn.Position = UDim2.new(0.1,0,0.5,0)
        btn.BackgroundColor3 = Color3.fromRGB(25,30,40)
        btn.BorderColor3 = Color3.fromRGB(255,80,180)
        btn.BorderSizePixel = 2
        btn.Text = "TP "..A.tpDistance.." Studs"
        btn.TextColor3 = Color3.fromRGB(240,240,240)
        btn.Font = Enum.Font.Code
        btn.TextSize = 14
        btn.ZIndex = 99999
        btn.Parent = g
        Instance.new("UICorner",btn).CornerRadius = UDim.new(0,5)
        SetupDraggable(btn)
        btn.MouseButton1Click:Connect(function() A.DoTeleport() end)
        A.tpBtnGui = g
    end

    function A.DestroyTPButton()
        if A.tpBtnGui then A.tpBtnGui:Destroy() A.tpBtnGui = nil end
    end

    function A.CreateFreezeButton()
        if A.freezeBtnGui then return end
        local g = Instance.new("ScreenGui")
        g.Name = "AstraFreezeUI"
        g.ResetOnSpawn = false
        pcall(function() g.Parent = GUI_PARENT end)
        if not g.Parent then g.Parent = LP:WaitForChild("PlayerGui") end
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0,110,0,45)
        btn.Position = UDim2.new(0.1,0,0.4,0)
        btn.BackgroundColor3 = Color3.fromRGB(30,35,45)
        btn.BorderColor3 = Color3.fromRGB(255,80,180)
        btn.BorderSizePixel = 2
        btn.Text = "Freeze Up"
        btn.TextColor3 = Color3.fromRGB(230,230,230)
        btn.Font = Enum.Font.SourceSansBold
        btn.TextSize = 16
        btn.ZIndex = 99999
        btn.Parent = g
        Instance.new("UICorner",btn).CornerRadius = UDim.new(0,6)
        SetupDraggable(btn)
        local pressed = false
        btn.MouseButton1Click:Connect(function()
            local char = LP.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            if not root then return end
            pressed = not pressed
            if pressed then
                A.freezeTargetCF = root.CFrame * CFrame.new(0,50,0)
                btn.Text = "UNFREEZE"
                btn.BackgroundColor3 = Color3.fromRGB(180,40,80)
                A.freezeEnabled = true
            else
                A.freezeEnabled = false
                A.freezeTargetCF = nil
                btn.Text = "Freeze Up"
                btn.BackgroundColor3 = Color3.fromRGB(30,35,45)
            end
        end)
        A.freezeBtnGui = g
    end

    function A.DestroyFreezeButton()
        if A.freezeBtnGui then A.freezeBtnGui:Destroy() A.freezeBtnGui = nil end
        A.freezeEnabled = false
        A.freezeTargetCF = nil
    end

    function A.CreateAirWalkButton()
        if A.awBtnGui then return end
        local g = Instance.new("ScreenGui")
        g.Name = "AstraAirWalkButton"
        g.ResetOnSpawn = false
        pcall(function() g.Parent = GUI_PARENT end)
        if not g.Parent then g.Parent = LP:WaitForChild("PlayerGui") end
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0,130,0,45)
        btn.Position = UDim2.new(0.1,0,0.55,0)
        btn.BackgroundColor3 = Color3.fromRGB(25,30,40)
        btn.BorderColor3 = Color3.fromRGB(255,80,180)
        btn.BorderSizePixel = 2
        btn.Text = "Air Walk"
        btn.TextColor3 = Color3.fromRGB(240,240,240)
        btn.Font = Enum.Font.Code
        btn.TextSize = 13
        btn.ZIndex = 99999
        btn.Parent = g
        Instance.new("UICorner",btn).CornerRadius = UDim.new(0,6)
        SetupDraggable(btn)
        btn.MouseButton1Click:Connect(function() A.DoAirWalk() end)
        A.awBtnGui = g
    end

    function A.DestroyAirWalkButton()
        if A.awBtnGui then A.awBtnGui:Destroy() A.awBtnGui = nil end
    end

    local KbGui = Instance.new("ScreenGui")
    KbGui.Name = "GradientKeybinds"
    KbGui.ResetOnSpawn = false
    pcall(function() KbGui.Parent = GUI_PARENT end)
    if not KbGui.Parent then KbGui.Parent = LP:WaitForChild("PlayerGui") end

    local KbMain = Instance.new("Frame")
    KbMain.Size = UDim2.new(0,240,0,120)
    KbMain.Position = UDim2.new(0.05,0,0.3,0)
    KbMain.BackgroundColor3 = Color3.fromRGB(255,255,255)
    KbMain.BackgroundTransparency = 0.15
    KbMain.BorderSizePixel = 0
    KbMain.Active = true
    KbMain.Draggable = true
    KbMain.Visible = false
    KbMain.Parent = KbGui
    local KbGrad = Instance.new("UIGradient",KbMain)
    KbGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(45,45,45)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(20,20,20)),
    })
    KbGrad.Rotation = 90

    local KbHeader = Instance.new("Frame")
    KbHeader.Size = UDim2.new(1,0,0,24)
    KbHeader.BackgroundColor3 = Color3.fromRGB(255,255,255)
    KbHeader.BorderSizePixel = 0
    KbHeader.Parent = KbMain
    local KbHG = Instance.new("UIGradient",KbHeader)
    KbHG.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(15,15,15)),
        ColorSequenceKeypoint.new(0.6, Color3.fromRGB(25,20,35)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(40,20,50)),
    })

    local KbTitle = Instance.new("TextLabel")
    KbTitle.Size = UDim2.new(1,-10,1,0)
    KbTitle.Position = UDim2.new(0,8,0,0)
    KbTitle.BackgroundTransparency = 1
    KbTitle.Text = "Keybinds"
    KbTitle.TextColor3 = Color3.fromRGB(220,220,220)
    KbTitle.Font = Enum.Font.Code
    KbTitle.TextSize = 12
    KbTitle.TextXAlignment = Enum.TextXAlignment.Left
    KbTitle.Parent = KbHeader

    local KbContainer = Instance.new("Frame")
    KbContainer.Size = UDim2.new(1,-16,1,-28)
    KbContainer.Position = UDim2.new(0,8,0,26)
    KbContainer.BackgroundTransparency = 1
    KbContainer.Parent = KbMain
    Instance.new("UIListLayout",KbContainer).Padding = UDim.new(0,2)

    A.UpdateKeybindOverlay = function()
        for _, c in ipairs(KbContainer:GetChildren()) do
            if c:IsA("TextLabel") then c:Destroy() end
        end
        local rows = {
            {"Aim", A.keybinds.SilentAim},
            {"Fly", A.keybinds.Fly},
            {"Freeze", A.keybinds.Freeze},
            {"TP", A.tpKey},
            {"AirWalk", A.airWalkKey},
        }
        for i, r in ipairs(rows) do
            local t = Instance.new("TextLabel")
            t.Size = UDim2.new(1,0,0,18)
            t.BackgroundTransparency = 1
            t.Text = "["..(r[2] and r[2].Name or "---").."]  "..r[1]
            t.TextColor3 = Color3.fromRGB(170,172,169)
            t.Font = Enum.Font.Code
            t.TextSize = 12
            t.TextXAlignment = Enum.TextXAlignment.Left
            t.LayoutOrder = i
            t.Parent = KbContainer
        end
    end

    function A.SetKeybindOverlayVisible(v)
        KbMain.Visible = v
        if v then A.UpdateKeybindOverlay() end
    end

    RS.RenderStepped:Connect(function()
        local vp = Camera.ViewportSize
        local cx = vp.X/2
        local cy = vp.Y/2

        FOVFrame.Position = UDim2.fromOffset(cx - A.FOV_RADIUS, cy - A.FOV_RADIUS)
        FOVFrame.Size = UDim2.fromOffset(A.FOV_RADIUS*2, A.FOV_RADIUS*2)
        FOVFrame.Visible = A.SHOW_FOV and A.SILENT_AIM
        FS.Color = A.silentAimFovColor

        if snapLine then
            if not A.SILENT_AIM or not A.SNAPLINE then
                snapLine.Visible = false
            else
                local t = A.getSilentTarget()
                if not t then
                    snapLine.Visible = false
                else
                    local pos, onS = Camera:WorldToViewportPoint(t.Position)
                    if not onS then
                        snapLine.Visible = false
                    else
                        snapLine.From = Vector2.new(cx, cy)
                        snapLine.To = Vector2.new(pos.X, pos.Y)
                        snapLine.Color = A.silentAimSnaplineColor
                        snapLine.Visible = true
                    end
                end
            end
        end

        for id, marker in pairs(A.ActiveHitmarkers) do
            local screenPos, onScreen = Camera:WorldToViewportPoint(marker.WorldPosition)
            if onScreen and marker.Opacity > 0 then
                local x = screenPos.X
                local y = screenPos.Y
                local sz = marker.Size
                local gap = marker.Gap
                marker.Lines[1].From = Vector2.new(x-gap, y-gap)
                marker.Lines[1].To = Vector2.new(x-gap-sz, y-gap-sz)
                marker.Lines[2].From = Vector2.new(x+gap, y-gap)
                marker.Lines[2].To = Vector2.new(x+gap+sz, y-gap-sz)
                marker.Lines[3].From = Vector2.new(x-gap, y+gap)
                marker.Lines[3].To = Vector2.new(x-gap-sz, y+gap+sz)
                marker.Lines[4].From = Vector2.new(x+gap, y+gap)
                marker.Lines[4].To = Vector2.new(x+gap+sz, y+gap+sz)
                for _,line in ipairs(marker.Lines) do
                    line.Transparency = marker.Opacity
                    line.Visible = true
                end
            else
                for _,line in ipairs(marker.Lines) do
                    line.Visible = false
                end
            end
        end

        for player, o in pairs(A.espCache) do
            local char = player.Character
            if A.espEnabled and char then
                local root = char:FindFirstChild("HumanoidRootPart")
                local head = char:FindFirstChild("Head")
                local hum = char:FindFirstChildOfClass("Humanoid")
                if root and head and hum and hum.Health > 0 then
                    local headPos, on1 = Camera:WorldToViewportPoint(head.Position + Vector3.new(0,0.5,0))
                    local legPos, on2 = Camera:WorldToViewportPoint(root.Position - Vector3.new(0,3,0))
                    if on1 and on2 then
                        local boxH = math.abs(headPos.Y - legPos.Y)
                        local boxW = boxH / 1.6
                        local bx = headPos.X - boxW/2
                        local by = headPos.Y
                        if A.espBox then
                            o.Box.Size = Vector2.new(boxW, boxH)
                            o.Box.Position = Vector2.new(bx, by)
                            o.Box.Color = A.espBoxColor
                            o.Box.Visible = true
                        else o.Box.Visible = false end
                        if A.espName then
                            o.Name.Text = player.Name
                            o.Name.Position = Vector2.new(headPos.X, by-16)
                            o.Name.Color = A.espNameColor
                            o.Name.Visible = true
                        else o.Name.Visible = false end
                        if A.espHealth then
                            local hp = math.clamp(hum.Health/hum.MaxHealth, 0, 1)
                            local barH = boxH * hp
                            o.HPBg.Size = Vector2.new(4, boxH)
                            o.HPBg.Position = Vector2.new(bx-6, by)
                            o.HPBg.Visible = true
                            o.HPBar.Size = Vector2.new(4, barH)
                            o.HPBar.Position = Vector2.new(bx-6, by+(boxH-barH))
                            o.HPBar.Color = Color3.fromHSV(hp*0.33, 1, 1)
                            o.HPBar.Visible = true
                        else
                            o.HPBg.Visible = false
                            o.HPBar.Visible = false
                        end
                        if A.espDistance then
                            local myRoot = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
                            local d = myRoot and math.floor((myRoot.Position - root.Position).Magnitude) or 0
                            o.Dist.Text = d.." studs"
                            o.Dist.Position = Vector2.new(headPos.X, legPos.Y+4)
                            o.Dist.Color = A.espDistColor
                            o.Dist.Visible = true
                        else o.Dist.Visible = false end
                        if A.espWeapon then
                            local tool = char:FindFirstChildOfClass("Tool")
                            o.Weapon.Text = tool and tool.Name or "Fists"
                            o.Weapon.Position = Vector2.new(headPos.X, legPos.Y+18)
                            o.Weapon.Color = A.espWeapColor
                            o.Weapon.Visible = true
                        else o.Weapon.Visible = false end
                    else
                        for _,d in pairs(o) do d.Visible = false end
                    end
                else
                    for _,d in pairs(o) do d.Visible = false end
                end
            else
                for _,d in pairs(o) do d.Visible = false end
            end
        end

        if A.cameraFovEnabled then
            local cam = workspace.CurrentCamera
            if cam and cam.FieldOfView ~= A.cameraFovValue then
                cam.FieldOfView = A.cameraFovValue
            end
        end
    end)

    local wasInAirBhop = false
    RS.Heartbeat:Connect(function()
        if A.noFogEnabled and A.Lighting.FogEnd ~= 1e6 then
            A.Lighting.FogEnd = 1e6
            A.Lighting.FogStart = 1e5
        end
        if A.fullbrightEnabled then
            if A.Lighting.Brightness ~= 3 then A.Lighting.Brightness = 3 end
            if A.Lighting.ClockTime ~= 14 then A.Lighting.ClockTime = 14 end
            if A.Lighting.GlobalShadows ~= false then A.Lighting.GlobalShadows = false end
        end

        local char = LP.Character
        if not char then return end
        local root = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not root or not hum then return end

        if A.noClipEnabled then
            for _,p in ipairs(char:GetDescendants()) do
                if p:IsA("BasePart") then p.CanCollide = false end
            end
        end
        if A.speedHackEnabled and hum.WalkSpeed ~= A.speedValue then
            hum.WalkSpeed = A.speedValue
        end
        if A.flyEnabled then
            local dir = Vector3.zero
            if UIS:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
            if UIS:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
            if UIS:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0,1,0) end
            if UIS:IsKeyDown(Enum.KeyCode.LeftShift) then dir = dir - Vector3.new(0,1,0) end
            root.Velocity = dir.Magnitude > 0 and dir.Unit * A.flySpeed or Vector3.zero
            hum.PlatformStand = true
        elseif hum.PlatformStand and not A.freezeEnabled and not A.airWalkBusy then
            hum.PlatformStand = false
        end
        if A.freezeEnabled and UIS:IsKeyDown(A.keybinds.Freeze) then
            if not A.freezeTargetCF then
                A.freezeTargetCF = root.CFrame * CFrame.new(0,50,0)
            end
            root.CFrame = A.freezeTargetCF
            root.Velocity = Vector3.zero
            hum.PlatformStand = true
        elseif A.freezeTargetCF then
            A.freezeTargetCF = nil
            hum.PlatformStand = false
        end
        if A.bhopEnabled then
            local state = hum:GetState()
            local inAir = state == Enum.HumanoidStateType.Freefall or state == Enum.HumanoidStateType.Jumping
            if not inAir and wasInAirBhop then hum.Jump = true end
            wasInAirBhop = inAir
        end
        if A.gunChamsEnabled then
            for _,tool in ipairs(char:GetChildren()) do
                if tool:IsA("Tool") then
                    for _,p in ipairs(tool:GetDescendants()) do
                        if p:IsA("BasePart") then
                            p.Color = A.gunChamsColor
                            p.Material = Enum.Material.Neon
                        end
                    end
                end
            end
        end
    end)

    UIS.JumpRequest:Connect(function()
        local char = LP.Character
        if not char then return end
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum and A.infJumpEnabled then
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)

    UIS.InputBegan:Connect(function(inp, gpe)
        if gpe then return end
        if inp.KeyCode == A.airWalkKey then A.DoAirWalk() end
        if inp.KeyCode == A.tpKey then A.DoTeleport() end
    end)
end

do
    local A = _G.A
    local LP = A.LP
    local ReplicatedStorage = A.ReplicatedStorage
    local SoundService = A.SoundService

    task.spawn(function()
        task.wait(2)
        pcall(function()
            local blaster = ReplicatedStorage:FindFirstChild("Blaster")
            if not blaster then return end

            local util = blaster:FindFirstChild("Utility")
            local castRaysFunc = nil
            if util then
                local cr = util:FindFirstChild("castRays")
                if cr then
                    local ok, f = pcall(require, cr)
                    if ok and type(f) == "function" then castRaysFunc = f end
                end
            end

            if castRaysFunc and type(hookfunction) == "function" then
                pcall(function()
                    local original
                    original = hookfunction(castRaysFunc, function(plr, origin, dirs, ...)
                        if plr ~= LP then
                            return original(plr, origin, dirs, ...)
                        end

                        local target = nil
                        if A.SILENT_AIM and A.getSilentTarget then
                            target = A.getSilentTarget()
                        end

                        if target and type(dirs) == "table" then
                            for k in pairs(dirs) do
                                dirs[k] = Vector3.new(0,0,-1)
                            end
                        end

                        local result = original(plr, origin, dirs, ...)

                        if not target then
                            return result
                        end

                        if A.TRACER and A.CreateTracer then
                            pcall(function() A.CreateTracer(origin, target.Position) end)
                        end

                        if A.HIT_CHANCE/100 > math.random() then
                            if type(result) == "table" then
                                if result.position then
                                    result.position = target.Position
                                    result.instance = target
                                    if target.Parent then
                                        result.taggedHumanoid = target.Parent:FindFirstChildOfClass("Humanoid")
                                    end
                                elseif result[1] and type(result[1]) == "table" then
                                    for i = 1, #result do
                                        if type(result[i]) == "table" and result[i].position then
                                            result[i].position = target.Position
                                            result[i].instance = target
                                            if target.Parent then
                                                result[i].taggedHumanoid = target.Parent:FindFirstChildOfClass("Humanoid")
                                            end
                                        end
                                    end
                                end
                            end

                            if A.CreateHitmarker then
                                pcall(function() A.CreateHitmarker(target.Position) end)
                            end

                            if A.HIT_SOUND then
                                task.spawn(function()
                                    local s = Instance.new("Sound")
                                    s.SoundId = "rbxassetid://"..A.HIT_SOUND_ID
                                    s.Volume = A.HIT_SOUND_VOL
                                    s.Parent = SoundService
                                    s:Play()
                                    task.wait(2)
                                    s:Destroy()
                                end)
                            end

                            return result
                        end

                        return result
                    end)
                end)
            end

            local scripts = blaster:FindFirstChild("Scripts")
            if not scripts then return end

            pcall(function()
                local getRD = util and util:FindFirstChild("getRayDirections")
                if getRD then
                    local original = require(getRD)
                    package.loaded[getRD] = function(cf, count, spread, seed)
                        if A.noSpreadEnabled or A.SILENT_AIM then
                            local dirs = table.create(count)
                            local dir = cf.LookVector
                            for i=1,count do dirs[i] = dir end
                            return dirs
                        end
                        return original(cf, count, spread, seed)
                    end
                end
            end)

            local okBc, BC = pcall(function()
                return require(scripts:FindFirstChild("BlasterController"))
            end)
            if not okBc or not BC then return end

            pcall(function()
                local recoiler = scripts:FindFirstChild("CameraRecoiler")
                if recoiler then
                    local CR = require(recoiler)
                    local oldRecoil = CR.recoil
                    CR.recoil = function(...)
                        if A.noRecoilEnabled then return end
                        return oldRecoil(...)
                    end
                end
            end)

            if BC.reload then
                local oldReload = BC.reload
                BC.reload = function(self)
                    if not (A.instantReloadEnabled or A.autoReloadEnabled) then return oldReload(self) end
                    if tick() - A.lastReloadTime < 0.15 then return end
                    A.lastReloadTime = tick()
                    local maxAmmo = self.blaster and self.blaster:GetAttribute("magazineSize") or 30
                    self.ammo = maxAmmo
                    self.reloading = false
                    pcall(function()
                        if self.guiController then
                            self.guiController:setAmmo(maxAmmo)
                            self.guiController:setReloading(false)
                        end
                    end)
                    pcall(function()
                        local remotes = blaster:FindFirstChild("Remotes")
                        local reloadRemote = remotes and remotes:FindFirstChild("Reload")
                        if reloadRemote then reloadRemote:FireServer(self.blaster) end
                    end)
                end
            end

            if BC.canShoot then
                local oldCanShoot = BC.canShoot
                BC.canShoot = function(self)
                    if A.noEquipDelayEnabled then
                        self.canShootAfterEquip = true
                        self.isWeaponBlocked = false
                    end
                    if (A.instantReloadEnabled or A.autoReloadEnabled) and self.ammo ~= nil and self.ammo <= 0 then
                        pcall(function() BC.reload(self) end)
                    end
                    if A.noEquipDelayEnabled or A.instantReloadEnabled or A.autoReloadEnabled then
                        local sc = _G.SprintController
                        local sprinting = sc and sc.isSprinting and sc.isSprinting()
                        local hasAmmo = (self.ammo and self.ammo > 0) or A.instantReloadEnabled or A.autoReloadEnabled
                        return self:isHumanoidAlive() and self.equipped and hasAmmo and not sprinting
                    end
                    return oldCanShoot(self)
                end
            end

            if BC.startShooting then
                local oldSS = BC.startShooting
                BC.startShooting = function(self)
                    if A.fullAutoEnabled then
                        pcall(function()
                            if self.blaster then self.blaster:SetAttribute("fireMode","Auto") end
                        end)
                    end
                    return oldSS(self)
                end
            end

            if BC.equip then
                local oldEquip = BC.equip
                BC.equip = function(self)
                    oldEquip(self)
                    if A.noEquipDelayEnabled then self.canShootAfterEquip = true end
                end
            end
        end)
    end)

    local function processToolForRapidFire(tool)
        if not A.RAPID_FIRE then return end
        if not tool or not tool:IsA("Tool") then return end
        for attrName, _ in pairs(tool:GetAttributes()) do
            if attrName:lower() == "rateoffire" then
                pcall(function() tool:SetAttribute(attrName, A.RAPID_VALUE) end)
            end
        end
    end

    local function hookCharForRapid(char)
        if not char then return end
        for _,item in ipairs(char:GetChildren()) do
            processToolForRapidFire(item)
        end
        char.ChildAdded:Connect(function(child)
            task.wait(0.05)
            processToolForRapidFire(child)
        end)
    end

    if LP.Character then hookCharForRapid(LP.Character) end
    LP.CharacterAdded:Connect(hookCharForRapid)

    task.spawn(function()
        while true do
            task.wait(0.5)
            if A.RAPID_FIRE and LP.Character then
                for _,item in ipairs(LP.Character:GetChildren()) do
                    processToolForRapidFire(item)
                end
            end
        end
    end)

    task.spawn(function()
        while true do
            task.wait(0.1)
            if A.instantHitEnabled then
                local char = LP.Character
                if char then
                    local tool = char:FindFirstChildOfClass("Tool")
                    if tool then
                        pcall(function()
                            for name, _ in pairs(tool:GetAttributes()) do
                                local lower = name:lower()
                                if lower == "bulletspeed" or lower == "bulletvelocity" or lower == "projectilespeed" then
                                    tool:SetAttribute(name, A.bulletSpeedValue)
                                end
                            end
                        end)
                    end
                end
            end
        end
    end)
end

task.defer(function()
    if _G.A and _G.A.CenterMain then
        _G.A.CenterMain()
    end
end)

print("[Astra v22.1] LOADED OK")
